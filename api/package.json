{
  "name": "zengram-api",
  "version": "4.5.0",
  "type": "module",
  "description": "Zengram memory API — pgvector + BM25 with RRF fusion, entity extraction, credential scrubbing.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "find tests -name '*.test.js' | xargs node --test",
    "typecheck": "tsc --noEmit -p jsconfig.json"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "express": "^4.21.0",
    "node-cron": "^4.2.1",
    "openai": "^4.73.0",
    "pg": "^8.13.0"
  }
}
